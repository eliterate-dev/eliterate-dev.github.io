<a class="@Classes" href="posts/tags/@id" style="background-color:hsl(@color);rotate:@(Rotate)deg;" tabindex="0">@Name</a>

@code {
    [Parameter]
    public required string Name { get; set; }
    [Parameter]
    public string Classes { get; set; } = "";
    [Parameter]
    public decimal Rotate { get; set; }
    private string color => $"{Name.GetIntFromString(360)}, 100%, 80%";
    private string id => Name.ToUrlSafeString();
}
